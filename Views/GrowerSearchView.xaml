<Window x:Class="WPFGrowerApp.Views.GrowerSearchView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFGrowerApp.Views"
        mc:Ignorable="d"
        Title="Grower Search" Height="450" Width="600"
        WindowStartupLocation="CenterScreen">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Search for Grower" 
                   FontSize="20" FontWeight="SemiBold" Margin="0,0,0,20"/>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <TextBox x:Name="SearchTextBox" Grid.Column="0" Margin="0,0,10,0"
                     FontSize="14" Padding="5"
                     Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="SearchTextBox_KeyDown"/>
            
            <Button Grid.Column="1" Content="Search" Padding="15,5"
                    Command="{Binding SearchCommand}"/>
        </Grid>

        <DataGrid Grid.Row="2" x:Name="ResultsDataGrid"
                  ItemsSource="{Binding SearchResults}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  SelectionChanged="ResultsDataGrid_SelectionChanged"
                  BorderThickness="1" BorderBrush="#DDDDDD"
                  RowHeaderWidth="0"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeRows="False"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  HorizontalGridLinesBrush="#EEEEEE"
                  AlternatingRowBackground="#F5F5F5"
                  HeadersVisibility="Column">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Number" Binding="{Binding GrowerNumber}" Width="80"/>
                <DataGridTextColumn Header="Name" Binding="{Binding GrowerName}" Width="*"/>
                <DataGridTextColumn Header="Cheque Name" Binding="{Binding ChequeName}" Width="*"/>
                <DataGridTextColumn Header="City" Binding="{Binding City}" Width="120"/>
                <DataGridTextColumn Header="Phone" Binding="{Binding Phone}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button Content="Cancel" Width="100" Margin="0,0,10,0" Click="CancelButton_Click"/>
            <Button Content="Select" Width="100" IsDefault="True" Click="SelectButton_Click"
                    IsEnabled="{Binding ElementName=ResultsDataGrid, Path=SelectedItem, Converter={StaticResource NullToBooleanConverter}}"/>
        </StackPanel>
    </Grid>
</Window>
