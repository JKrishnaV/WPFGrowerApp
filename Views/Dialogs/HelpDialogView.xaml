<UserControl x:Class="WPFGrowerApp.Views.Dialogs.HelpDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFGrowerApp.Views.Dialogs"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:HelpDialogViewModel}"
             d:DesignHeight="600" d:DesignWidth="700" 
             MinWidth="700" MaxWidth="800" MinHeight="400">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="*"/>     <!-- Content -->
            <RowDefinition Height="Auto"/>  <!-- Close Button -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <materialDesign:PackIcon Kind="HelpCircle" 
                                   Width="32" Height="32" 
                                   VerticalAlignment="Center"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"
                                   Margin="0,0,10,0"/>
            <TextBlock Text="{Binding Title}" 
                       Style="{StaticResource MaterialDesignHeadline5TextBlock}"
                       VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Content Area -->
        <ScrollViewer Grid.Row="1" 
                      VerticalScrollBarVisibility="Auto"
                      Margin="0,0,0,16">
            <StackPanel>
                <!-- Main Content -->
                <materialDesign:Card Padding="20" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                   Text="Overview"
                                   Margin="0,0,0,10"
                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        <TextBlock Text="{Binding Content}"
                                   TextWrapping="Wrap"
                                   LineHeight="22"
                                   Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Quick Tips Section -->
                <materialDesign:Card Padding="20" 
                                   Margin="0,0,0,16"
                                   Background="{DynamicResource PrimaryHueLightBrush}"
                                   Visibility="{Binding QuickTips, Converter={StaticResource NotNullOrEmptyToVisibilityConverter}}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <materialDesign:PackIcon Kind="Lightbulb" 
                                                   Width="24" Height="24"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                            <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                       Text="Quick Tips"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding QuickTips}"
                                   TextWrapping="Wrap"
                                   LineHeight="22"
                                   Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                    </StackPanel>
                </materialDesign:Card>

                <!-- Keyboard Shortcuts Section -->
                <materialDesign:Card Padding="20"
                                   Visibility="{Binding KeyboardShortcuts, Converter={StaticResource NotNullOrEmptyToVisibilityConverter}}">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                            <materialDesign:PackIcon Kind="Keyboard" 
                                                   Width="24" Height="24"
                                                   VerticalAlignment="Center"
                                                   Margin="0,0,8,0"
                                                   Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                            <TextBlock Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                                       Text="Keyboard Shortcuts"
                                       VerticalAlignment="Center"
                                       Foreground="{DynamicResource PrimaryHueMidBrush}"/>
                        </StackPanel>
                        <TextBlock Text="{Binding KeyboardShortcuts}"
                                   TextWrapping="Wrap"
                                   LineHeight="22"
                                   Style="{StaticResource MaterialDesignBody1TextBlock}"/>
                    </StackPanel>
                </materialDesign:Card>
            </StackPanel>
        </ScrollViewer>

        <!-- Close Button -->
        <Button Grid.Row="2"
                Content="Close"
                Command="{Binding CloseCommand}"
                Style="{StaticResource MaterialDesignRaisedButton}"
                HorizontalAlignment="Right"
                Width="100"
                IsDefault="True"
                IsCancel="True"/>
    </Grid>
</UserControl>

