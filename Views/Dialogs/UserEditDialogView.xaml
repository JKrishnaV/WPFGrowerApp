<UserControl x:Class="WPFGrowerApp.Views.Dialogs.UserEditDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFGrowerApp.Views.Dialogs"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels.Dialogs"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:WPFGrowerApp.Converters"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:UserEditDialogViewModel}"
             d:DesignHeight="550" d:DesignWidth="600" MaxWidth="650" MinWidth="600"
>

    <UserControl.Resources>
        <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
    </UserControl.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title with unsaved changes indicator -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="{Binding Title}" 
                       Style="{StaticResource MaterialDesignHeadline6TextBlock}"/>
            <TextBlock Text="*" 
                       Foreground="Red" 
                       FontSize="20"
                       FontWeight="Bold"
                       Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        </StackPanel>

        <!-- Unsaved Changes Warning -->
        <Border Grid.Row="1" 
                Background="Orange" 
                CornerRadius="4" 
                Padding="10" 
                Margin="0,0,0,10"
                Visibility="{Binding ShowWarningBanner, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <materialDesign:PackIcon Kind="Warning" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                    <TextBlock Text="You have unsaved changes. Are you sure you want to discard them?" FontWeight="SemiBold"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Yes, Discard" 
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Command="{Binding ConfirmDiscardCommand}"
                            Margin="0,0,8,0"/>
                    <Button Content="No, Keep Changes" 
                            Style="{StaticResource MaterialDesignFlatButton}"
                            Command="{Binding CancelDiscardCommand}"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Form Fields -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Username -->
                <TextBox materialDesign:HintAssist.Hint="Username *"
                         Text="{Binding Username, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                         IsEnabled="{Binding CanEditUsername}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,8,0,8"
                         MaxLength="50"/>

                <!-- Full Name -->
                <TextBox materialDesign:HintAssist.Hint="Full Name *"
                         Text="{Binding FullName, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,8,0,8"
                         MaxLength="100"/>

                <!-- Email -->
                <TextBox materialDesign:HintAssist.Hint="Email (Optional)"
                         Text="{Binding Email, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True}"
                         Style="{StaticResource MaterialDesignOutlinedTextBox}"
                         Margin="0,8,0,8"
                         MaxLength="100"/>

                <!-- Role -->
                <ComboBox materialDesign:HintAssist.Hint="Role"
                          ItemsSource="{Binding Roles}"
                          SelectedItem="{Binding SelectedRole, UpdateSourceTrigger=PropertyChanged}"
                          DisplayMemberPath="RoleName"
                          IsEnabled="{Binding CanEditRole}"
                          Style="{StaticResource MaterialDesignOutlinedComboBox}"
                          Margin="0,8,0,8"/>

                <!-- Password Fields (only for new users) -->
                <StackPanel Visibility="{Binding ShowPasswordFields, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <PasswordBox x:Name="PasswordBox"
                                 materialDesign:HintAssist.Hint="Password *"
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 Margin="0,8,0,8"
                                 MaxLength="100"
                                 PasswordChanged="PasswordBox_PasswordChanged"/>

                    <PasswordBox x:Name="ConfirmPasswordBox"
                                 materialDesign:HintAssist.Hint="Confirm Password *"
                                 Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                                 Margin="0,8,0,8"
                                 MaxLength="100"
                                 PasswordChanged="ConfirmPasswordBox_PasswordChanged"/>

                    <!-- Password Requirements -->
                    <Border Background="{DynamicResource MaterialDesignDivider}"
                            CornerRadius="4"
                            Padding="10"
                            Margin="0,8,0,8">
                        <StackPanel>
                            <TextBlock Text="Password Requirements:" 
                                       FontWeight="SemiBold"
                                       FontSize="12"
                                       Margin="0,0,0,4"/>
                            <TextBlock Text="• At least 8 characters" FontSize="11" Opacity="0.8"/>
                            <TextBlock Text="• At least one uppercase letter" FontSize="11" Opacity="0.8"/>
                            <TextBlock Text="• At least one lowercase letter" FontSize="11" Opacity="0.8"/>
                            <TextBlock Text="• At least one digit" FontSize="11" Opacity="0.8"/>
                            <TextBlock Text="• At least one special character" FontSize="11" Opacity="0.8"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <!-- Active Status -->
                <CheckBox Content="Active" 
                          IsChecked="{Binding IsActive}"
                          Style="{StaticResource MaterialDesignCheckBox}"
                          Margin="0,16,0,8"/>

                <!-- Info for edit mode -->
                <Border Visibility="{Binding IsEditMode, Converter={StaticResource BooleanToVisibilityConverter}}"
                        Background="{DynamicResource MaterialDesignDivider}"
                        CornerRadius="4"
                        Padding="10"
                        Margin="0,8,0,0">
                    <StackPanel>
                        <TextBlock Text="Note: Password reset must be done through a separate process." 
                                   FontSize="11" 
                                   FontStyle="Italic"
                                   TextWrapping="Wrap"
                                   Opacity="0.7"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right" 
                    Margin="0,16,0,0">
            <Button Content="Cancel" 
                    IsCancel="True" 
                    Style="{StaticResource MaterialDesignOutlinedButton}" 
                    Command="{Binding CancelCommand}"
                    Width="100"/>

            <Button Content="Save" 
                    IsDefault="True" 
                    Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding SaveCommand}" 
                    Margin="8,0,0,0"
                    Width="100"/>
        </StackPanel>
    </Grid>
</UserControl>

