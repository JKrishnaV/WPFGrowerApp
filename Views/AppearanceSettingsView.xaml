
<UserControl x:Class="WPFGrowerApp.Views.AppearanceSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:WPFGrowerApp.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:AppearanceSettingsViewModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Margin="20">
        <TextBlock Text="Appearance Settings" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>

        <GroupBox Header="Font Size" Margin="0,0,0,20">
            <StackPanel Margin="10">
                <TextBlock Text="Adjust the application font size scaling." Margin="0,0,0,10"/>
                <Slider Minimum="0.8" Maximum="2.0" Value="{Binding FontScalingFactor, Mode=TwoWay}" TickFrequency="0.1" IsSnapToTickEnabled="True"/>
                <TextBlock Text="{Binding FontScalingFactor, StringFormat='{}{0:N2}x'}" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>
        </GroupBox>

        <GroupBox Header="Login Settings" Margin="0,0,0,20">
            <StackPanel Margin="10">
                <TextBlock Text="Configure login behavior and preferences." Margin="0,0,0,10"/>
                <CheckBox Content="Remember Password" 
                          IsChecked="{Binding RememberPassword, Mode=TwoWay}"
                          Margin="0,5,0,0"/>
                <TextBlock Text="When enabled, the login form will remember your password preference." 
                           FontSize="12" 
                           Foreground="Gray" 
                           Margin="0,5,0,0"/>
            </StackPanel>
        </GroupBox>

        <Button Content="Apply Changes" Command="{Binding ApplyChangesCommand}" HorizontalAlignment="Right"/>
    </StackPanel>
</UserControl>
