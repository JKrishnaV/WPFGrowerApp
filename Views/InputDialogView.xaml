<UserControl x:Class="WPFGrowerApp.Views.InputDialogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFGrowerApp.Views"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:InputDialogViewModel, IsDesignTimeCreatable=False}"
             Background="{DynamicResource MaterialDesignPaper}"
             MinWidth="400"
             MinHeight="200">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="Auto"/> <!-- Message -->
            <RowDefinition Height="Auto"/> <!-- Input -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Modern Title (No Header Bar) -->
        <TextBlock Grid.Row="0" 
                   Text="{Binding Title}"
                   Style="{StaticResource MaterialDesignHeadline6TextBlock}"
                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                   Margin="0,0,0,16"/>

        <!-- Message -->
        <TextBlock Grid.Row="1" 
                   Text="{Binding Message}"
                   Style="{StaticResource MaterialDesignBody1TextBlock}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"/>

        <!-- Input Field -->
        <Grid Grid.Row="2" Margin="0,0,0,24">
            <TextBox x:Name="InputTextBox"
                     Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                     materialDesign:HintAssist.Hint="{Binding PlaceholderText}"
                     Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                     AcceptsReturn="{Binding IsMultiline}"
                     TextWrapping="{Binding IsMultiline, Converter={StaticResource BooleanToTextWrappingConverter}}"
                     VerticalScrollBarVisibility="{Binding IsMultiline, Converter={StaticResource BooleanToScrollBarVisibilityConverter}}"
                     MinHeight="{Binding IsMultiline, Converter={StaticResource BooleanToHeightConverter}}"
                     MaxLength="500"/>
        </Grid>

        <!-- Modern Action Buttons -->
        <StackPanel Grid.Row="3" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,8,0,0">
            
            <Button Style="{StaticResource MaterialDesignFlatButton}"
                    Command="{Binding CancelCommand}"
                    Content="CANCEL"
                    Margin="0,0,8,0"
                    Padding="16,8"/>
            
            <Button Style="{StaticResource MaterialDesignRaisedButton}"
                    Command="{Binding OkCommand}"
                    Content="OK"
                    Padding="16,8"
                    IsDefault="True"/>
        </StackPanel>
    </Grid>
</UserControl>

