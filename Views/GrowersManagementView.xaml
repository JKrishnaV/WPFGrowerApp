<UserControl x:Class="WPFGrowerApp.Views.GrowersManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WPFGrowerApp.Views"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels"
             xmlns:converters="clr-namespace:WPFGrowerApp.Converters"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type={x:Type vm:GrowersManagementViewModel}}"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/StandardizedViewTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <converters:NotNullConverter x:Key="NotNullConverter"/>
            <converters:NotNullOrEmptyToVisibilityConverter x:Key="NotNullOrEmptyToVisibilityConverter"/>
            <converters:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
            <converters:StatusToColorConverter x:Key="StatusToColorConverter"/>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Breadcrumb -->
            <RowDefinition Height="Auto"/> <!-- Title -->
            <RowDefinition Height="Auto"/> <!-- Filters/Actions Panel -->
            <RowDefinition Height="Auto"/> <!-- Main Content Area -->
            <RowDefinition Height="Auto"/> <!-- Status/Footer Bar -->
        </Grid.RowDefinitions>

        <!-- Breadcrumb Navigation -->
        <materialDesign:ColorZone Grid.Row="0" Style="{StaticResource BreadcrumbNavigationStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Command="{Binding NavigateToDashboardCommand}"
                        Padding="5,0">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Home" Width="16" Height="16" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <TextBlock Text="Dashboard" FontSize="12"/>
                    </StackPanel>
                </Button>
                <materialDesign:PackIcon Kind="ChevronRight" Width="16" Height="16" VerticalAlignment="Center" Margin="5,0"/>
                <TextBlock Text="Growers" FontSize="12" VerticalAlignment="Center" FontWeight="SemiBold"/>
            </StackPanel>
        </materialDesign:ColorZone>

        <!-- View Title -->
        <TextBlock Grid.Row="1" 
                   Text="Grower Management" 
                   Style="{StaticResource StandardizedViewTitleStyle}"/>

        <!-- Filters/Actions Panel -->
        <materialDesign:Card Grid.Row="2" Style="{StaticResource FiltersActionsPanelStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <!-- Search Row -->
                    <RowDefinition Height="Auto"/> <!-- Filters Row -->
                    <RowDefinition Height="Auto"/> <!-- Action Buttons Row -->
                </Grid.RowDefinitions>

                <!-- Search Row -->
                <Grid Grid.Row="0" Style="{StaticResource SearchRowStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBox Grid.Column="0"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             materialDesign:HintAssist.Hint="Search growers by name, number, or city..."
                             Style="{StaticResource MaterialDesignOutlinedTextBox}"
                             Margin="0,0,10,0">
                        <TextBox.InputBindings>
                            <KeyBinding Key="Enter" Command="{Binding SearchCommand}"/>
                        </TextBox.InputBindings>
                    </TextBox>
                    
                    <Button Grid.Column="1"
                            Command="{Binding SearchCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Width="80"
                            Margin="0,0,10,0">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Magnify" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="Search"/>
                        </StackPanel>
                    </Button>
                    
                    <Button Grid.Column="2"
                            Command="{Binding ClearSearchCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}"
                            Width="80"
                            Margin="0,0,10,0">
                        <TextBlock Text="Clear"/>
                    </Button>
                    
                    <Button Grid.Column="3"
                            Command="{Binding AddNewGrowerCommand}"
                            Style="{StaticResource MaterialDesignRaisedButton}"
                            Width="100">
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="Plus" Width="16" Height="16" Margin="0,0,5,0"/>
                            <TextBlock Text="Add New"/>
                        </StackPanel>
                    </Button>
                </Grid>

                <!-- Filters Row -->
                <Grid Grid.Row="1" Style="{StaticResource FiltersRowStyle}">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="150"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <TextBlock Grid.Column="0" Text="Filters:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    
                    <ComboBox Grid.Column="1"
                              ItemsSource="{Binding DateRangeOptions}"
                              SelectedItem="{Binding SelectedDateRange}"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Margin="0,0,10,0"/>
                    
                    <TextBlock Grid.Column="2" Text="Status:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    
                    <ComboBox Grid.Column="3"
                              ItemsSource="{Binding StatusOptions}"
                              SelectedItem="{Binding SelectedStatus}"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"
                              Margin="0,0,10,0"/>
                    
                    <TextBlock Grid.Column="4" Text="Location:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                    
                    <ComboBox Grid.Column="5"
                              ItemsSource="{Binding LocationOptions}"
                              SelectedItem="{Binding SelectedLocation}"
                              DisplayMemberPath="DisplayName"
                              Style="{StaticResource MaterialDesignOutlinedComboBox}"/>
                </Grid>

                <!-- Action Buttons Row -->
                <StackPanel Grid.Row="2" Style="{StaticResource ActionButtonRowStyle}">
                    <Button Content="Edit"
                            Command="{Binding EditGrowerCommand}"
                            Style="{StaticResource SecondaryActionButtonStyle}"
                            IsEnabled="{Binding SelectedGrower, Converter={StaticResource NotNullConverter}}"/>
                    <Button Content="Delete"
                            Command="{Binding DeleteGrowerCommand}"
                            Style="{StaticResource DangerActionButtonStyle}"
                            IsEnabled="{Binding SelectedGrower, Converter={StaticResource NotNullConverter}}"/>
                    <Button Content="View Details"
                            Command="{Binding ViewGrowerDetailsCommand}"
                            Style="{StaticResource SecondaryActionButtonStyle}"
                            IsEnabled="{Binding SelectedGrower, Converter={StaticResource NotNullConverter}}"/>
                    <Button Content="Export"
                            Command="{Binding ExportGrowersCommand}"
                            Style="{StaticResource SecondaryActionButtonStyle}"/>
                    <Button Content="Refresh"
                            Command="{Binding RefreshCommand}"
                            Style="{StaticResource MaterialDesignOutlinedButton}">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <materialDesign:PackIcon Kind="Refresh"/>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>
                </StackPanel>
            </Grid>
        </materialDesign:Card>

        <!-- Main Content Area -->
        <materialDesign:Card Grid.Row="3" Style="{StaticResource MainContentAreaStyle}" Margin="0,5,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Growers DataGrid -->
                <DataGrid x:Name="GrowersDataGrid"
                          Grid.Row="0"
                          ItemsSource="{Binding FilteredGrowers}"
                          SelectedItem="{Binding SelectedGrower, Mode=TwoWay}"
                          Style="{StaticResource StandardizedDataGridStyle}"
                          MaxHeight="400"
                          VerticalScrollBarVisibility="Auto"
                          MouseDoubleClick="GrowersDataGrid_MouseDoubleClick">
                    
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" 
                                          Binding="{Binding GrowerNumber}" 
                                          Width="60"
                                          SortDirection="Ascending"/>
                        <DataGridTextColumn Header="Name" 
                                          Binding="{Binding GrowerName}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="Cheque Name" 
                                          Binding="{Binding ChequeName}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="City" 
                                          Binding="{Binding City}" 
                                          Width="120"/>
                        <DataGridTextColumn Header="Phone" 
                                          Binding="{Binding Phone}" 
                                          Width="150"/>
                        <DataGridTemplateColumn Header="Status" Width="100">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Background="{Binding Status, Converter={StaticResource StatusToColorConverter}}"
                                            Style="{StaticResource StatusBadgeStyle}">
                                        <TextBlock Text="{Binding Status}" 
                                                   Foreground="White"
                                                   FontSize="11"
                                                   FontWeight="SemiBold"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </materialDesign:Card>

        <!-- Status/Footer Bar -->
        <materialDesign:ColorZone Grid.Row="4" Style="{StaticResource StatusFooterBarStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="Status: " FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding StatusMessage}"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2" Orientation="Horizontal">
                    <TextBlock Text="Records: " FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding RecordCount}"/>
                    <TextBlock Text=" | " Margin="5,0"/>
                    <TextBlock Text="Last Updated: " FontWeight="SemiBold"/>
                    <TextBlock Text="{Binding LastUpdated}"/>
                </StackPanel>
            </Grid>
        </materialDesign:ColorZone>

        <!-- Loading Indicator -->
        <materialDesign:Card Grid.Row="0" 
                            Grid.RowSpan="5"
                            Style="{StaticResource LoadingIndicatorStyle}"
                            Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel Orientation="Horizontal">
                <ProgressBar Style="{StaticResource MaterialDesignCircularProgressBar}"
                            Value="0"
                            IsIndeterminate="True"/>
                <TextBlock Text="Loading growers..."
                          VerticalAlignment="Center"
                          Margin="10,0,0,0"/>
            </StackPanel>
        </materialDesign:Card>
    </Grid>
</UserControl>
