<UserControl x:Class="WPFGrowerApp.Views.ReportsCardsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFGrowerApp.Views"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:ReportsCardsViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource ThemeMainBackground}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Report Card Style -->
            <Style x:Key="ReportCardStyle" TargetType="materialDesign:Card">
                <Setter Property="Margin" Value="8"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
                <Setter Property="MinHeight" Value="160"/>
                <Setter Property="MaxHeight" Value="200"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Report Card Button Style -->
            <Style x:Key="ReportCardButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{DynamicResource MaterialDesignSelection}"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Category Header Style -->
            <Style x:Key="CategoryHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource MaterialDesignBody}"/>
                <Setter Property="Margin" Value="0,20,0,10"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" 
                Background="{DynamicResource MaterialDesignCardBackground}" 
                Padding="20,15" 
                Margin="0,0,0,20"
                CornerRadius="8"
                BorderBrush="{DynamicResource MaterialDesignDivider}"
                BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="📊 Reports &amp; Analytics" 
                            Foreground="{DynamicResource MaterialDesignBody}" 
                            FontSize="24" 
                            FontWeight="SemiBold" 
                            HorizontalAlignment="Center"/>
                <TextBlock Text="Generate comprehensive reports and analytics for your berry farm operations" 
                            Foreground="{DynamicResource MaterialDesignBodyLight}" 
                            FontSize="14" 
                            HorizontalAlignment="Center"
                            Margin="0,5,0,0"/>
            </StackPanel>
        </Border>
        
        <!-- Reports Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                
                <!-- Analytics Reports -->
                <TextBlock Text="📈 Analytics &amp; Charts" Style="{StaticResource CategoryHeaderStyle}"/>
                <ItemsControl ItemsSource="{Binding AnalyticsReports}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Style="{StaticResource ReportCardStyle}">
                                <Button Command="{Binding DataContext.NavigateToReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ReportCardButtonStyle}">
                                    <Grid Margin="20">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="60"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Icon -->
                                        <Border Grid.Row="0" Width="60" Height="60" 
                                                Background="{Binding IconColor}" CornerRadius="30" 
                                                HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="{Binding IconKind}" 
                                                                     Width="32" Height="32" 
                                                                     Foreground="White"
                                                                     HorizontalAlignment="Center"
                                                                     VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <!-- Title -->
                                        <TextBlock Grid.Row="1" Text="{Binding Title}" 
                                                   FontWeight="SemiBold" FontSize="16" 
                                                   HorizontalAlignment="Center" Margin="0,15,0,8"
                                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                                        
                                        <!-- Description -->
                                        <TextBlock Grid.Row="2" Text="{Binding Description}" 
                                                   FontSize="12" 
                                                   HorizontalAlignment="Center" Margin="0,0,0,8"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   TextWrapping="Wrap"
                                                   TextAlignment="Center"/>
                                        
                                        <!-- Status -->
                                        <Border Grid.Row="3" Background="{Binding StatusColor}" 
                                                CornerRadius="12" Padding="8,4" HorizontalAlignment="Center"
                                                Margin="0,8,0,0">
                                            <TextBlock Text="{Binding StatusText}" 
                                                       FontSize="11" FontWeight="Bold" 
                                                       Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </Button>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Financial Reports -->
                <TextBlock Text="💰 Financial Reports" Style="{StaticResource CategoryHeaderStyle}"/>
                <ItemsControl ItemsSource="{Binding FinancialReports}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Style="{StaticResource ReportCardStyle}">
                                <Button Command="{Binding DataContext.NavigateToReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ReportCardButtonStyle}">
                                    <Grid Margin="20">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="60"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Icon -->
                                        <Border Grid.Row="0" Width="60" Height="60" 
                                                Background="{Binding IconColor}" CornerRadius="30" 
                                                HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="{Binding IconKind}" 
                                                                     Width="32" Height="32" 
                                                                     Foreground="White"
                                                                     HorizontalAlignment="Center"
                                                                     VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <!-- Title -->
                                        <TextBlock Grid.Row="1" Text="{Binding Title}" 
                                                   FontWeight="SemiBold" FontSize="16" 
                                                   HorizontalAlignment="Center" Margin="0,15,0,8"
                                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                                        
                                        <!-- Description -->
                                        <TextBlock Grid.Row="2" Text="{Binding Description}" 
                                                   FontSize="12" 
                                                   HorizontalAlignment="Center" Margin="0,0,0,8"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   TextWrapping="Wrap"
                                                   TextAlignment="Center"/>
                                        
                                        <!-- Status -->
                                        <Border Grid.Row="3" Background="{Binding StatusColor}" 
                                                CornerRadius="12" Padding="8,4" HorizontalAlignment="Center"
                                                Margin="0,8,0,0">
                                            <TextBlock Text="{Binding StatusText}" 
                                                       FontSize="11" FontWeight="Bold" 
                                                       Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </Button>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
                <!-- Operational Reports -->
                <TextBlock Text="⚙️ Operational Reports" Style="{StaticResource CategoryHeaderStyle}"/>
                <ItemsControl ItemsSource="{Binding OperationalReports}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <UniformGrid Columns="3"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <materialDesign:Card Style="{StaticResource ReportCardStyle}">
                                <Button Command="{Binding DataContext.NavigateToReportCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                        CommandParameter="{Binding}"
                                        Style="{StaticResource ReportCardButtonStyle}">
                                    <Grid Margin="20">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="60"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="*"/>
                                        </Grid.RowDefinitions>
                                        
                                        <!-- Icon -->
                                        <Border Grid.Row="0" Width="60" Height="60" 
                                                Background="{Binding IconColor}" CornerRadius="30" 
                                                HorizontalAlignment="Center" VerticalAlignment="Center">
                                            <materialDesign:PackIcon Kind="{Binding IconKind}" 
                                                                     Width="32" Height="32" 
                                                                     Foreground="White"
                                                                     HorizontalAlignment="Center"
                                                                     VerticalAlignment="Center"/>
                                        </Border>
                                        
                                        <!-- Title -->
                                        <TextBlock Grid.Row="1" Text="{Binding Title}" 
                                                   FontWeight="SemiBold" FontSize="16" 
                                                   HorizontalAlignment="Center" Margin="0,15,0,8"
                                                   Foreground="{DynamicResource MaterialDesignBody}"/>
                                        
                                        <!-- Description -->
                                        <TextBlock Grid.Row="2" Text="{Binding Description}" 
                                                   FontSize="12" 
                                                   HorizontalAlignment="Center" Margin="0,0,0,8"
                                                   Foreground="{DynamicResource MaterialDesignBodyLight}"
                                                   TextWrapping="Wrap"
                                                   TextAlignment="Center"/>
                                        
                                        <!-- Status -->
                                        <Border Grid.Row="3" Background="{Binding StatusColor}" 
                                                CornerRadius="12" Padding="8,4" HorizontalAlignment="Center"
                                                Margin="0,8,0,0">
                                            <TextBlock Text="{Binding StatusText}" 
                                                       FontSize="11" FontWeight="Bold" 
                                                       Foreground="White"/>
                                        </Border>
                                    </Grid>
                                </Button>
                            </materialDesign:Card>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                
            </StackPanel>
        </ScrollViewer>
        
        <!-- Loading Indicator -->
        <Grid Grid.Row="1" Background="{DynamicResource MaterialDesignCardBackground}" 
              Opacity="0.9" 
              Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="4"/>
                <TextBlock Text="Loading reports..." 
                           Foreground="{DynamicResource MaterialDesignBody}" 
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
        
    </Grid>
</UserControl>
