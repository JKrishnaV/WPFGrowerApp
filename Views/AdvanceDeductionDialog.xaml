<Window x:Class="WPFGrowerApp.Views.AdvanceDeductionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:WPFGrowerApp.Converters"
        mc:Ignorable="d"
        Title="Advance Deductions" 
        Height="600" 
        Width="900"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/StandardizedViewTemplate.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <converters:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
            <converters:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
            <converters:NotNullOrEmptyToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            
            <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="16"/>
                <Setter Property="FontWeight" Value="Bold"/>
                <Setter Property="Margin" Value="0,0,0,10"/>
            </Style>
            
            <Style x:Key="SummaryTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Margin" Value="0,5"/>
            </Style>
            
            <Style x:Key="WarningTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="Red"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
            
            <Style x:Key="SuccessTextStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="12"/>
                <Setter Property="Foreground" Value="Green"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
            </Style>
        </ResourceDictionary>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="Advance Deductions" 
                   Style="{StaticResource HeaderTextStyle}"/>

        <!-- Summary Information -->
        <Border Grid.Row="1" 
                Background="#F0F0F0" 
                Padding="15" 
                Margin="0,0,0,15" 
                CornerRadius="5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <TextBlock Text="Total Payment Amount:" Style="{StaticResource SummaryTextStyle}"/>
                    <TextBlock Text="{Binding TotalAmount, StringFormat=C}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
                
                <StackPanel Grid.Column="1">
                    <TextBlock Text="Total Deductions:" Style="{StaticResource SummaryTextStyle}"/>
                    <TextBlock Text="{Binding TotalDeductionAmount, StringFormat=C}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
                
                <StackPanel Grid.Column="2">
                    <TextBlock Text="Remaining Payment:" Style="{StaticResource SummaryTextStyle}"/>
                    <TextBlock Text="{Binding TotalRemainingPayment, StringFormat=C}" FontSize="16" FontWeight="Bold"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Deductions DataGrid -->
        <DataGrid Grid.Row="2" 
                  ItemsSource="{Binding AdvanceDeductions}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserReorderColumns="False"
                  CanUserResizeColumns="True"
                  CanUserSortColumns="True"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  SelectionMode="Single"
                  SelectionUnit="FullRow"
                  Margin="0,0,0,15">
            
            <DataGrid.Columns>
                <!-- Advance Date -->
                <DataGridTextColumn Header="Advance Date" 
                                    Binding="{Binding AdvanceDate}" 
                                    Width="100" 
                                    IsReadOnly="True"/>
                
                <!-- Original Amount -->
                <DataGridTextColumn Header="Original Amount" 
                                    Binding="{Binding OriginalAmountDisplay}" 
                                    Width="120" 
                                    IsReadOnly="True"/>
                
                <!-- Remaining Amount -->
                <DataGridTextColumn Header="Remaining Amount" 
                                    Binding="{Binding RemainingAmountDisplay}" 
                                    Width="120" 
                                    IsReadOnly="True"/>
                
                <!-- Suggested Deduction -->
                <DataGridTextColumn Header="Suggested" 
                                    Binding="{Binding SuggestedDeductionDisplay}" 
                                    Width="100" 
                                    IsReadOnly="True"/>
                
                <!-- Actual Deduction -->
                <DataGridTemplateColumn Header="Actual Deduction" Width="120">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding ActualDeduction, UpdateSourceTrigger=PropertyChanged}"
                                     IsEnabled="{Binding CanModify}"
                                     Background="{Binding IsValidDeduction, Converter={StaticResource BoolToBrushConverter}}"
                                     BorderBrush="{Binding IsValidDeduction, Converter={StaticResource BoolToBrushConverter}}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <!-- Remaining After Deduction -->
                <DataGridTextColumn Header="Remaining After" 
                                    Binding="{Binding RemainingAfterDeductionDisplay}" 
                                    Width="120" 
                                    IsReadOnly="True"/>
                
                <!-- Status -->
                <DataGridTemplateColumn Header="Status" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Ellipse Width="10" Height="10" 
                                         Fill="{Binding IsValidDeduction, Converter={StaticResource BoolToBrushConverter}}"
                                         Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding StatusDisplay}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                
                <!-- Actions -->
                <DataGridTemplateColumn Header="Actions" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="Reset" 
                                        Command="{Binding DataContext.ModifyDeductionCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}"
                                        Margin="0,0,5,0"
                                        Padding="5,2"
                                        FontSize="10"/>
                                <Button Content="Max" 
                                        Click="SetToRemaining_Click"
                                        Margin="0,0,5,0"
                                        Padding="5,2"
                                        FontSize="10"/>
                                <Button Content="Clear" 
                                        Click="ClearDeduction_Click"
                                        Padding="5,2"
                                        FontSize="10"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Validation Messages -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="Validation Messages:" Style="{StaticResource SummaryTextStyle}"/>
            <ItemsControl ItemsSource="{Binding AdvanceDeductions}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ValidationMessage}" 
                                   Style="{StaticResource WarningTextStyle}"
                                   Visibility="{Binding ValidationMessage, Converter={StaticResource StringToVisibilityConverter}}"/>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button Content="Apply Deductions" 
                    Command="{Binding ApplyDeductionsCommand}"
                    IsEnabled="{Binding CanApplyDeductions}"
                    Margin="0,0,10,0"
                    Padding="15,5"
                    Background="#4CAF50"
                    Foreground="White"
                    FontWeight="SemiBold"/>
            <Button Content="Clear All" 
                    Command="{Binding ClearDeductionsCommand}"
                    Margin="0,0,10,0"
                    Padding="15,5"
                    Background="#FF9800"
                    Foreground="White"
                    FontWeight="SemiBold"/>
            <Button Content="Cancel" 
                    IsCancel="True"
                    Padding="15,5"
                    Background="#F44336"
                    Foreground="White"
                    FontWeight="SemiBold"/>
        </StackPanel>
    </Grid>
</Window>
