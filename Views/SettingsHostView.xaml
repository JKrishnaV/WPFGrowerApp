<UserControl x:Class="WPFGrowerApp.Views.SettingsHostView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFGrowerApp.Views"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type={x:Type vm:SettingsHostViewModel}, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800"
             Background="{DynamicResource ThemeMainBackground}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Styles/Styles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            
            <!-- Enhanced Color Palette -->
            <SolidColorBrush x:Key="PrimaryBlue" Color="#1976D2"/>
            <SolidColorBrush x:Key="PrimaryBlueLight" Color="#E3F2FD"/>
            <SolidColorBrush x:Key="SecondaryGreen" Color="#4CAF50"/>
            <SolidColorBrush x:Key="AccentOrange" Color="#FF6B35"/>
            <SolidColorBrush x:Key="WarningYellow" Color="#FFC107"/>
            <SolidColorBrush x:Key="ErrorRed" Color="#F44336"/>
            <SolidColorBrush x:Key="BackgroundGray" Color="#FAFAFA"/>
            
            <!-- Enhanced Action Card Style -->
            <Style x:Key="EnhancedActionCardStyle" TargetType="materialDesign:Card">
                <Setter Property="Margin" Value="8"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp2"/>
                <Setter Property="MinHeight" Value="140"/>
                <Setter Property="MaxHeight" Value="180"/>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="materialDesign:ElevationAssist.Elevation" Value="Dp4"/>
                    </Trigger>
                </Style.Triggers>
            </Style>
            
            <!-- Enhanced Action Button Style -->
            <Style x:Key="EnhancedActionButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="0"/>
                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="12"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="{StaticResource PrimaryBlueLight}"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="{StaticResource PrimaryBlue}"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            
            <!-- Enhanced Breadcrumb Style -->
            <Style x:Key="EnhancedBreadcrumbStyle" TargetType="materialDesign:ColorZone">
                <Setter Property="Mode" Value="PrimaryLight"/>
                <Setter Property="Background" Value="{StaticResource PrimaryBlueLight}"/>
                <Setter Property="Padding" Value="20,15"/>
                <Setter Property="Margin" Value="0,0,0,20"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
            
            <!-- Dark Theme Colors -->
            <SolidColorBrush x:Key="DarkCardBackground" Color="#2D2D30"/>
            <SolidColorBrush x:Key="DarkCardBorder" Color="#3F3F46"/>
            <SolidColorBrush x:Key="DarkCardHoverBackground" Color="#3E3E42"/>
            <SolidColorBrush x:Key="DarkCardHoverBorder" Color="#007ACC"/>
            <SolidColorBrush x:Key="DarkTextPrimary" Color="#E0E0E0"/>
            <SolidColorBrush x:Key="DarkTextSecondary" Color="#A0A0A0"/>
            <SolidColorBrush x:Key="DarkAccent" Color="#BB86FC"/>
            <SolidColorBrush x:Key="DarkHeaderBackground" Color="#1E1E1E"/>
            <SolidColorBrush x:Key="DarkMainBackground" Color="#1E1E1E"/>
            
            <!-- Light Theme Colors -->
            <SolidColorBrush x:Key="LightCardBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="LightCardBorder" Color="#CCCCCC"/>
            <SolidColorBrush x:Key="LightCardHoverBackground" Color="#F5F5F5"/>
            <SolidColorBrush x:Key="LightCardHoverBorder" Color="#3F51B5"/>
            <SolidColorBrush x:Key="LightTextPrimary" Color="#212121"/>
            <SolidColorBrush x:Key="LightTextSecondary" Color="#757575"/>
            <SolidColorBrush x:Key="LightAccent" Color="#3F51B5"/>
            <SolidColorBrush x:Key="LightHeaderBackground" Color="#3F51B5"/>
            <SolidColorBrush x:Key="LightMainBackground" Color="#FAFAFA"/>
            
            <!-- Dynamic Theme Resources (will be updated by code-behind) -->
            <SolidColorBrush x:Key="CardBackground" Color="#FFFFFF"/>
            <SolidColorBrush x:Key="CardBorder" Color="#CCCCCC"/>
            <SolidColorBrush x:Key="CardHoverBackground" Color="#F5F5F5"/>
            <SolidColorBrush x:Key="CardHoverBorder" Color="#3F51B5"/>
            <SolidColorBrush x:Key="TextPrimary" Color="#212121"/>
            <SolidColorBrush x:Key="TextSecondary" Color="#757575"/>
            <SolidColorBrush x:Key="AccentColor" Color="#3F51B5"/>
            <SolidColorBrush x:Key="HeaderBackground" Color="#3F51B5"/>
            
            <!-- Header Text Style - Theme Aware -->
            <Style x:Key="DarkHeaderTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="{DynamicResource ScaledFontSizeHeader}"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            </Style>
            
            <!-- Body Text Style - Theme Aware -->
            <Style x:Key="DarkBodyTextStyle" TargetType="TextBlock">
                <Setter Property="FontFamily" Value="Segoe UI"/>
                <Setter Property="FontSize" Value="{DynamicResource ScaledFontSizeNormal}"/>
                <Setter Property="Foreground" Value="{DynamicResource TextPrimary}"/>
            </Style>
        </ResourceDictionary>
    </UserControl.Resources>

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Enhanced Breadcrumb -->
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Welcome Message -->
            <RowDefinition Height="*"/>     <!-- Settings Cards Grid -->
        </Grid.RowDefinitions>
        
        <!-- Enhanced Breadcrumb Navigation -->
        <materialDesign:ColorZone Grid.Row="0" Style="{StaticResource EnhancedBreadcrumbStyle}">
            <StackPanel Orientation="Horizontal">
                <Button Style="{StaticResource MaterialDesignFlatButton}"
                        Padding="8,4"
                        Command="{Binding DataContext.NavigateToDashboardCommand, RelativeSource={RelativeSource AncestorType={x:Type Window}}}">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Home" Width="20" Height="20" 
                                               VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Dashboard" FontSize="14" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
                
                <materialDesign:PackIcon Kind="ChevronRight" Width="20" Height="20" 
                                       VerticalAlignment="Center" Margin="15,0" Opacity="0.6"/>
                
                <Border Background="{StaticResource AccentOrange}" 
                        CornerRadius="20" Padding="15,8" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal">
                        <materialDesign:PackIcon Kind="Cog" Width="20" Height="20" 
                                               VerticalAlignment="Center" Margin="0,0,8,0" 
                                               Foreground="White"/>
                        <TextBlock Text="Settings" FontSize="14" FontWeight="SemiBold" 
                                   Foreground="White"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </materialDesign:ColorZone>
        
        <!-- Header - Theme Aware -->
        <Border Grid.Row="1" 
                Background="{DynamicResource HeaderBackground}" 
                Padding="20,15" 
                Margin="0,0,0,20"
                CornerRadius="8"
                BorderBrush="{DynamicResource CardBorder}"
                BorderThickness="0,0,0,2">
            <TextBlock Text="⚙️ Settings" 
                        Foreground="White" 
                        FontSize="{DynamicResource ScaledFontSizeHeader}" 
                        FontWeight="SemiBold" 
                        HorizontalAlignment="Center"/>
        </Border>
        
        <!-- Welcome Message -->
        <StackPanel Grid.Row="2" Margin="0,0,0,25">
            <TextBlock Text="Application Configuration" 
                       Style="{StaticResource DarkHeaderTextStyle}"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Configure your application settings, users, and system preferences" 
                       Style="{StaticResource DarkBodyTextStyle}"
                       HorizontalAlignment="Center"
                       Foreground="{DynamicResource TextSecondary}"/>
        </StackPanel>
        
        <!-- Settings Cards Grid -->
        <ScrollViewer Grid.Row="3" VerticalScrollBarVisibility="Auto">
    <Grid>
        <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Security Settings Row -->
                <materialDesign:Card Grid.Row="0" Grid.Column="0" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToChangePasswordCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource AccentOrange}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Key" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Change Password" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Update your account password" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource SecondaryGreen}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Security" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- User Management Card -->
                <materialDesign:Card Grid.Row="0" Grid.Column="1" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToUserManagementCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource PrimaryBlue}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="AccountMultiple" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Manage Users" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Add, edit, and manage user accounts" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource AccentOrange}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Admin Only" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Products Card -->
                <materialDesign:Card Grid.Row="0" Grid.Column="2" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToProductsCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource SecondaryGreen}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="PackageVariant" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Products" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Configure product types and categories" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource SecondaryGreen}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Configuration" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Process Types Card -->
                <materialDesign:Card Grid.Row="1" Grid.Column="0" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToProcessTypesCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource WarningYellow}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Cog" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Process Types" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Define processing methods and workflows" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource WarningYellow}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="System" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Pricing Card -->
                <materialDesign:Card Grid.Row="1" Grid.Column="1" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToPricingCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource AccentOrange}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Cash" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Pricing" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Set up pricing schedules and rates" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource AccentOrange}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Financial" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Depots Card -->
                <materialDesign:Card Grid.Row="1" Grid.Column="2" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToDepotsCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource SecondaryGreen}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Store" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Depots" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Manage depot locations and settings" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource SecondaryGreen}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Configuration" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Payment Groups Card -->
                <materialDesign:Card Grid.Row="2" Grid.Column="0" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToPaymentGroupsCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource PrimaryBlue}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="AccountGroup" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
                            </Border>
                            
                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Payment Groups" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Configure payment groups and methods" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource AccentOrange}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Financial" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Container Types Card -->
                <materialDesign:Card Grid.Row="2" Grid.Column="1" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToContainerTypesCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource WarningYellow}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Package" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
        </Border>

                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Container Types" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Define container types and specifications" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource WarningYellow}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="System" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>

                <!-- Appearance Card -->
                <materialDesign:Card Grid.Row="2" Grid.Column="2" Style="{StaticResource EnhancedActionCardStyle}">
                    <Button Command="{Binding NavigateToAppearanceCommand}"
                            Style="{StaticResource EnhancedActionButtonStyle}">
                        <Grid Margin="20">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            
                            <!-- Icon with background circle -->
                            <Border Grid.Row="0" Width="60" Height="60" 
                                    Background="{StaticResource AccentOrange}" CornerRadius="30" 
                                    HorizontalAlignment="Center" VerticalAlignment="Center">
                                <materialDesign:PackIcon Kind="Palette" 
                                                         Width="32" Height="32" 
                                                         Foreground="White"
                                                         HorizontalAlignment="Center"
                                                         VerticalAlignment="Center"/>
        </Border>

                            <!-- Title -->
                            <TextBlock Grid.Row="1" Text="Appearance" 
                                       FontWeight="SemiBold" FontSize="16" 
                                       HorizontalAlignment="Center" Margin="0,15,0,8"/>
                            
                            <!-- Description -->
                            <TextBlock Grid.Row="2" Text="Customize application appearance and themes" 
                                       FontSize="13" Opacity="0.8" TextWrapping="Wrap" 
                                       HorizontalAlignment="Center" Margin="0,0,0,12"/>
                            
                            <!-- Status indicator -->
                            <Border Grid.Row="3" Background="{StaticResource SecondaryGreen}" 
                                    CornerRadius="12" Padding="12,6" HorizontalAlignment="Center"
                                    Margin="0,8,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Direction="270" ShadowDepth="2" 
                                                    Opacity="0.3" BlurRadius="4"/>
                                </Border.Effect>
                                <TextBlock Text="Personalization" FontSize="13" FontWeight="Bold" 
                                           Foreground="White"/>
                            </Border>
                        </Grid>
                    </Button>
                </materialDesign:Card>
            </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
