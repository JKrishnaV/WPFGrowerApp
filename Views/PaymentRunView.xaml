<UserControl x:Class="WPFGrowerApp.Views.PaymentRunView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:WPFGrowerApp.Views"
             xmlns:vm="clr-namespace:WPFGrowerApp.ViewModels"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:PaymentRunViewModel, IsDesignTimeCreatable=False}"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="{DynamicResource MaterialDesignPaper}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Style="{StaticResource MaterialDesignHeadline5TextBlock}" Text="Run Advance Payment" Margin="0,0,0,16"/>

        <!-- Parameters Section -->
        <materialDesign:Card Grid.Row="1" Padding="16" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Left Column -->
                <ComboBox Grid.Row="0" Grid.Column="0" Margin="0,5"
                          materialDesign:HintAssist.Hint="Advance Number (1, 2, or 3)"
                          Style="{StaticResource MaterialDesignFloatingHintComboBox}"
                          SelectedValue="{Binding AdvanceNumber}">
                    <ComboBoxItem Content="1"/>
                    <ComboBoxItem Content="2"/>
                    <ComboBoxItem Content="3"/>
                </ComboBox>

                <DatePicker Grid.Row="1" Grid.Column="0" Margin="0,5"
                            materialDesign:HintAssist.Hint="Payment Date"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            SelectedDate="{Binding PaymentDate}"/>

                <DatePicker Grid.Row="2" Grid.Column="0" Margin="0,5"
                            materialDesign:HintAssist.Hint="Include Receipts Up To"
                            Style="{StaticResource MaterialDesignFloatingHintDatePicker}"
                            SelectedDate="{Binding CutoffDate}"/>

                <TextBox Grid.Row="3" Grid.Column="0" Margin="0,5"
                         materialDesign:HintAssist.Hint="Crop Year"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding CropYear}"/>

                <TextBox Grid.Row="4" Grid.Column="0" Margin="0,5"
                         materialDesign:HintAssist.Hint="Product ID (Optional)"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding ProductId}"/>

                <TextBox Grid.Row="5" Grid.Column="0" Margin="0,5"
                         materialDesign:HintAssist.Hint="Process ID (Optional)"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding ProcessId}"/>

                <!-- Right Column -->
                 <TextBox Grid.Row="0" Grid.Column="2" Margin="0,5"
                         materialDesign:HintAssist.Hint="Include Grower ID (Optional)"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding IncludeGrowerId}"/>

                 <TextBox Grid.Row="1" Grid.Column="2" Margin="0,5"
                         materialDesign:HintAssist.Hint="Include Pay Group (Optional)"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding IncludePayGroup}"/>

                 <TextBox Grid.Row="2" Grid.Column="2" Margin="0,5"
                         materialDesign:HintAssist.Hint="Exclude Grower ID (Optional)"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding ExcludeGrowerId}"/>

                 <TextBox Grid.Row="3" Grid.Column="2" Margin="0,5"
                         materialDesign:HintAssist.Hint="Exclude Pay Group (Optional)"
                         Style="{StaticResource MaterialDesignFloatingHintTextBox}"
                         Text="{Binding ExcludePayGroup}"/>

                <!-- Run Button -->
                 <Button Grid.Row="6" Grid.Column="0" Grid.ColumnSpan="3" Margin="0,16,0,0"
                        Style="{StaticResource MaterialDesignRaisedButton}"
                        Command="{Binding StartPaymentRunCommand}"
                        IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBooleanConverter}}"
                        Content="Start Payment Run"/>

            </Grid>
        </materialDesign:Card>

        <!-- Log/Status Section -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
             <TextBlock Grid.Row="0" Style="{StaticResource MaterialDesignSubtitle1TextBlock}" Text="Run Log:" Margin="0,0,0,8"/>
             <ListBox Grid.Row="1" ItemsSource="{Binding RunLog}" Margin="0,0,0,8" BorderThickness="1" BorderBrush="{DynamicResource MaterialDesignDivider}" VerticalAlignment="Stretch" HorizontalContentAlignment="Stretch"/>
        </Grid>

        <StatusBar Grid.Row="3" VerticalAlignment="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
             <StatusBarItem HorizontalAlignment="Right">
                <ProgressBar Width="100" Height="15" IsIndeterminate="{Binding IsRunning}" Visibility="{Binding IsRunning, Converter={StaticResource BooleanToVisibilityConverter}}"/>
            </StatusBarItem>
        </StatusBar>

    </Grid>
</UserControl>
